<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="questionStyle">
        <item name="android:textColor">@android:color/black</item>
        <item name="android:textSize">@dimen/stdTextSize</item>
        <item name="android:gravity">center</item>
        <item name="android:padding">@dimen/smallPadding</item>
        <item name="android:background">@color/colorPrimary</item>
    </style>

    <style name="cardStyle">
        <item name="android:layout_margin">@dimen/cardMargin</item>
        <item name="cardCornerRadius">@dimen/cardRadius</item>
        <item name="cardElevation">@dimen/cardElevation</item>
        <item name="cardBackgroundColor">@color/cardBkground</item>
    </style>

    <style name="answerGroupStyle">
        <item name="android:textColor">@android:color/black</item>
        <item name="android:textSize">@dimen/stdTextSize</item>
        <item name="android:layout_marginLeft">@dimen/smallMargin</item>
        <item name="android:padding">@dimen/smallPadding</item>
    </style>

    <style name="answerTextinputStyle">
        <item name="android:textColor">@android:color/black</item>
        <item name="android:textSize">@dimen/stdTextSize</item>
        <item name="android:paddingLeft">@dimen/stdPadding</item>
        <item name="android:paddingRight">@dimen/stdPadding</item>
        <item name="android:layout_marginBottom">@dimen/smallMargin</item>
    </style>

    <style name="styledButton" parent="Widget.AppCompat.Button.Colored">
        <item name="android:textColor">@android:color/black</item>
        <item name="android:layout_marginBottom">@dimen/smallMargin</item>
    </style>
</resources>
